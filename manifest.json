{
  "$schema": "https://raw.githubusercontent.com/anthropics/mcpb/main/schemas/mcpb-manifest-v0.4.schema.json",
  "manifest_version": "0.4",
  "name": "styleforge",
  "display_name": "Styleforge",
  "version": "0.7.1",
  "description": "Per-author writing-style management via MCP.",
  "long_description": "Maintain isolated writing-style libraries for one or more authors in any language. Ingest new corpus with deduplication and snapshots. Get on-demand writing guides via MCP tools and `/style-write` slash commands. Includes feedback intake and rollback for misingested corpus.",
  "author": {
    "name": "styleforge"
  },
  "license": "GPL-3.0-or-later",
  "homepage": "https://github.com/coding-commits/styleforge",
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/server/index.js"
      ],
      "env": {
        "STYLEFORGE_HOME": "${user_config.data_dir}"
      }
    }
  },
  "user_config": {
    "data_dir": {
      "type": "string",
      "title": "Data directory",
      "description": "Where styleforge stores authors, corpus, snapshots, and changelog (default: ~/.styleforge). The server also needs to read source files from arbitrary paths during ingest, so this must NOT be sandboxed.",
      "required": false,
      "default": "${HOME}/.styleforge"
    }
  },
  "tools": [
    {
      "name": "list_authors",
      "description": "List all writing-style authors registered in styleforge."
    },
    {
      "name": "create_author",
      "description": "Register a new author with a slug and display name."
    },
    {
      "name": "delete_author",
      "description": "Permanently delete an author and all their data. Requires explicit confirmation."
    },
    {
      "name": "get_writing_guide",
      "description": "Return the complete writing guide for a given author: SKILL_OVERLAY + style-patterns + learned-rules. Call this BEFORE writing in an author's style."
    },
    {
      "name": "sample_corpus",
      "description": "Return signature passages from up to 10 corpus entries, bucketed by topic. Each passage is a short context-agnostic style exemplar (≤120 chars) demonstrating syntax/rhythm/rhetoric without content leakage."
    },
    {
      "name": "ingest_dryrun",
      "description": "Inspect files for ingest without writing. Returns lists of new files, exact duplicates, near-duplicates, and errors. ALWAYS call this before ingest_execute and show the result to the user."
    },
    {
      "name": "ingest_execute",
      "description": "Actually ingest files into an author's corpus. Creates a snapshot before writing. Refuses near-duplicates unless allow_near is set."
    },
    {
      "name": "record_pattern_evidence",
      "description": "After viewing an ingested article, record its topic tags and which style-patterns it exhibits. This is how an author's style fingerprint accumulates."
    },
    {
      "name": "record_signature_passages",
      "description": "Store 3-5 context-agnostic style exemplar passages (≤120 chars each) for a corpus entry. Demonstrates syntax/rhythm/rhetoric without topical content. Called during enrichment."
    },
    {
      "name": "append_observation",
      "description": "Append a pattern observation to observations.md. For patterns spotted in an article that aren't yet in style-patterns.md. Once recorded and referenced in record_pattern_evidence, they appear in style-patterns.md on next recompute_stats."
    },
    {
      "name": "recompute_stats",
      "description": "Recompute pattern frequencies from corpus-index.json. Run after record_pattern_evidence calls."
    },
    {
      "name": "get_stats",
      "description": "Show corpus size, topic breakdown, and pattern frequencies for an author."
    },
    {
      "name": "create_snapshot",
      "description": "Manually create a snapshot of an author's mutable state."
    },
    {
      "name": "list_snapshots",
      "description": "List all snapshots for an author, with timestamps and labels."
    },
    {
      "name": "rollback",
      "description": "Restore an author's state from a snapshot. Saves current state as a new snapshot first, so rollback-of-rollback works."
    },
    {
      "name": "record_feedback",
      "description": "Append a feedback entry about a recent style-write output."
    },
    {
      "name": "get_feedback_log",
      "description": "Read the feedback log for an author."
    },
    {
      "name": "apply_learned_rule",
      "description": "After style-feedback skill negotiates a learned rule with the user, persist it to learned-rules.md. Requires user-confirmed rule text."
    },
    {
      "name": "save_draft",
      "description": "Save a style-write draft as a .md file under the author's drafts/ directory. Returns the absolute file path."
    },
    {
      "name": "export_author",
      "description": "Export one or more authors as a .tar.gz archive. Includes corpus, patterns, observations, learned-rules. Excludes snapshots, drafts, and OS junk. Use all=true to export every author."
    },
    {
      "name": "import_author",
      "description": "Import authors from a .tar.gz export archive. Specify slugs to import selectively, or all=true for everything. Existing authors are skipped unless overwrite=true."
    },
    {
      "name": "check_update",
      "description": "Check if a newer version of styleforge is available on GitHub. Called automatically at the start of every operation."
    }
  ],
  "prompts": [
    {
      "name": "style-write",
      "description": "Write or rewrite a piece in a specific author's style.",
      "text": "I want you to write in the style of a specific styleforge author.\n\n1. Call `list_authors`. If there's only one, use it. Otherwise ask which one.\n2. Call `get_writing_guide` for that slug. Read the returned guide carefully.\n3. Call `sample_corpus` (k=5-8) — returns signature passages: short, context-agnostic style exemplars showing syntax/rhythm/rhetoric. Use these to calibrate voice.\n4. Ask the user what they want written (if they haven't already said).\n5. Draft the piece. Replicate STRUCTURE / SYNTAX / RHETORIC ONLY. Do NOT import the author's political stance unless explicitly requested. The user's stated stance always wins. Signature passages inform HOW to write, not WHAT to write — never leak their content into the output.\n6. Self-check against the §4 \"failure modes\" section of style-patterns.md.\n7. Call `save_draft` with the final text. Show the user the returned file path.\n8. Deliver. Then briefly ask: \"Satisfied? If not, tell me what's off and I'll log it via record_feedback.\""
    },
    {
      "name": "style-ingest",
      "description": "Ingest new articles into an author's corpus. Provide local file paths.",
      "text": "Ingest articles into a styleforge author's corpus.\n\n1. Call `list_authors`. If there's only one, use it. Otherwise ask.\n2. Ask for local file paths on the user's machine. Files must be accessible from the local filesystem.\n3. Call `ingest_dryrun`. Show: new_files / exact_duplicates / near_duplicates. If near-dups, ask: (a) treat as new, (b) skip, (c) cancel.\n4. After confirmation, call `ingest_execute` with a descriptive `message` parameter.\n5. Ingest Plan: display a summary showing total articles to annotate, batch layout (groups of <=3 articles, <=100k words per batch, split long ones into shorter chunks), then proceed automatically without asking for approval.\n6. Enrichment (parallel batched): launch one parallel agent per batch (<=3 articles each). Each agent reads its articles and performs: (A) multi-layer patterns (syntax/rhetoric/structure/register/rhythm), (B) n-gram fingerprints (recurring phrases, sentence starters, catchphrases — record directly, no cross-article threshold), (C) emotional arc (tonal trajectory mapping), (D) signature passages — extract 3-5 short (≤120 char) context-agnostic passages per article that demonstrate the author's syntax/rhythm/rhetoric WITHOUT topical content (strip proper nouns, dates, domain references). Calls `record_pattern_evidence`, `record_signature_passages`, and `append_observation`. Wait for all agents to complete.\n7. Call `recompute_stats`.\n8. Report: ingested count, snapshot id, rollback hint."
    },
    {
      "name": "style-feedback",
      "description": "Digest accumulated feedback into actionable learned-rules.",
      "text": "Process accumulated feedback for a styleforge author.\n\n1. Call `list_authors`. If there's only one, use it. Otherwise ask.\n2. Call `get_feedback_log` to read all entries.\n3. Call `create_snapshot` with label `pre-feedback-review`.\n4. Group recurring failure modes. Contradictory feedback: show to user, don't auto-resolve.\n5. Present proposed learned-rules: trigger / corrective_action / confidence.\n6. Wait for user accept/reject/edit on each.\n7. For accepted proposals, call `apply_learned_rule`.\n8. Report: K accepted, K rejected, snapshot id."
    },
    {
      "name": "style-rollback",
      "description": "Roll back an author's state to a previous snapshot.",
      "text": "Help the user roll back a styleforge author to a previous state.\n\n1. Call `list_authors`. If there's only one, use it. Otherwise ask.\n2. Call `list_snapshots`.\n3. Present numbered list (newest first) with timestamp and label/message.\n4. Ask user which to restore.\n5. Call `rollback`. Report success and pre-rollback snapshot name."
    },
    {
      "name": "style-authors",
      "description": "List all registered styleforge authors.",
      "text": "Call `list_authors` and present the results as a concise table (slug, display name)."
    },
    {
      "name": "style-stats",
      "description": "Show corpus statistics for a styleforge author.",
      "text": "Show statistics for a styleforge author.\n\n1. Call `list_authors`. If there's only one, use it. Otherwise ask.\n2. Call `get_stats`.\n3. Present: entry count, topic breakdown, pattern frequencies, sample_warning if applicable."
    },
    {
      "name": "style-export",
      "description": "Export one or more authors as a portable JSON bundle. Use -A to export all.",
      "text": "Export styleforge authors to a portable JSON bundle.\n\n1. Call `list_authors` to show available authors.\n2. Ask which authors to export, or if user said `-A` / `all`, export all.\n3. Optionally ask for an output path (default: ~/styleforge-export-<timestamp>.json).\n4. Call `export_author` with the chosen slugs (or all=true).\n5. Report the output file path and how many authors were exported."
    },
    {
      "name": "style-import",
      "description": "Import authors from a previously exported JSON bundle. Use -A to import all.",
      "text": "Import styleforge authors from a JSON export bundle.\n\n1. Ask for the path to the export .json file.\n2. Call `import_author` with the path and no slugs/all to discover available authors.\n3. If user said `-A` / `all`, set all=true. Otherwise ask which to import.\n4. If any authors already exist, ask about overwrite.\n5. Call `import_author` with final parameters.\n6. Report: imported / skipped / overwritten counts."
    },
    {
      "name": "style-help",
      "description": "Show the styleforge user guide — from ingestion to writing to feedback.",
      "text": "Print the styleforge user guide directly to the user. Do NOT call any MCP tools. Cover: create author, ingest corpus (with signature passages), write (auto-saves .md), feedback/learned-rules, stats, rollback, export/import. List all /style-* commands in a table."
    }
  ],
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=18.0.0"
    }
  }
}
